// The home spike for the player
const ME_HOME = 24;

// The home spike for the opponent
const OPPONENT_HOME = -1;

// Player numbers
const ME = 0;
const OPPONENT = 1;

// Game phase constants
const NOT_STARTED = 0;
const INITIAL_ROLLS = 1;
const PLAY = 2;
const FINISHED = 3;

export {
  ME_HOME,
  OPPONENT_HOME,
  ME,
  OPPONENT,
  NOT_STARTED,
  INITIAL_ROLLS,
  PLAY,
  FINISHED,
}
